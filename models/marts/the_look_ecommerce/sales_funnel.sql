/* 
aquisition (per event type)
awareness (event_type in "product", "department", "home")
consideration (event_type = "cart")
conversion (event_type = "purchase")
post-purchase (status in orders: processing -> shipped -> complete -> returned/cancelled)


Format:
|date|traffic_source|stage_name|value|

*/